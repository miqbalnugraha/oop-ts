interface Student {
    readonly name: string;
    readonly age: number;
}

let student: Student = {name: "Nugraha", age: 25};
// ketika properti sudah di-assign value nya, value tersebut tidak bisa diubah lagi.